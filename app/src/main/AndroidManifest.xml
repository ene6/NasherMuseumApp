<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name=".RackContents"
            android:label="Rack Contents"></activity>
        <activity
            android:name=".ScanRackActivity"
            android:label="Scan Rack" />
        <activity
            android:name=".LoginPageActivity"
            android:label="Login"/>
        <activity
            android:name=".SearchActivity"
            android:label="Search" />
        <activity
            android:name=".SubmissionActivity"
            android:label="Submit" />
        <activity
            android:name=".ObjectDataActivity"
            android:label="Object Data">

        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Object Scan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PictureCapture"
            android:screenOrientation="fullSensor"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="stateAlwaysHidden" />
    </application>

</manifest>